# LLVM_VE_PREFIX

BINS=${LLVM_VE_PREFIX}/bin
LIBS=${LLVM_VE_PREFIX}/lib
INCS=${LLVM_VE_PREFIX}/include
MODULES=${LLVM_VE_PREFIX}/lib/cmake

export PATH=${BINS}:${PATH}
export LIBRARY_PATH=${LIBS}:${LIBRARY_PATH}
export LD_LIBRARY_PATH=${LIBS}:${LD_LIBRARY_PATH}
export CPATH=${INCS}:${CPATH}
export CMAKE_MODULE_PATH=${MODULES}:${CMAKE_MODULE_PATH}
