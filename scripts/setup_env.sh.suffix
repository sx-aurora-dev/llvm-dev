# LLVM_VE_PREFIX

BINS=${LLVM_VE_PREFIX}/bin
LIBS=${LLVM_VE_PREFIX}/lib
INCS=${LLVM_VE_PREFIX}/include
MODULES=${LLVM_VE_PREFIX}/lib/cmake

PATH=${BINS}:${PATH}
LIBRARY_PATH=${LIBS}:${LIBRARY_PATH}
LD_LIBRARY_PATH=${LIBS}:${LD_LIBRARY_PATH}
CPATH=${INCS}:${CPATH}
CMAKE_MODULE_PATH=${MODULES}:${CMAKE_MODULE_PATH}
